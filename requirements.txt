# Headless-only requirements (no GUI).
# PyQt6 is excluded — the container runs simulations, not the launcher.

numpy
opencv-python-headless
scipy
scikit-image
torch --index-url https://download.pytorch.org/whl/cpu
torchvision --index-url https://download.pytorch.org/whl/cpu
matplotlib
tqdm
